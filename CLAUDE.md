@AGENTS.md

<!-- `next dev` writes a "This is NOT the Next.js you know" block below this
     line and re-adds it if removed. It is left here deliberately: the tool that
     maintains it owns the file it lives in, so there is exactly one copy and no
     conflict with AGENTS.md. Everything else belongs in AGENTS.md. -->
